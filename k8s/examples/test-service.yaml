apiVersion: v1
kind: Service
metadata:
  name: test-service
spec:
  selector:
    app: test-app
  type: NodePort
  ports:
  - port: 8080 # Service的端口,Service的8080端口映射到Pod的80端口
    targetPort: 80 # Pod的端口
    nodePort: 31000 #映射主机端口，不指定则在30000-32767范围分配
